# Surface

Single source for the OrdaX desktop, shell, windows, explorer, settings, design tokens and shared user-facing interaction behavior.

The same Surface source must serve Web, USB and native-disk OrdaX.

No target-specific visual fork is allowed. Environment differences must be resolved through capability interfaces under `system/adapters/`.
