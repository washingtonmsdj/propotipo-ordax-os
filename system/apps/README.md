# Apps

Single source for first-party OrdaX applications.

Apps must consume shared Surface/design primitives and capability contracts. Do not create separate Web and native app implementations unless a capability is truly unavailable; even then, keep one app owner and degrade/replace only the capability adapter.
