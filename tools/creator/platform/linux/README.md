# Linux Creator Adapter

Thin adapter for Linux raw-disk enumeration/open/write/elevation and native host integration.

It consumes the same Creator core contract as every other host and must not own a different partition/layout/security policy.
