# Windows Creator Adapter

Thin adapter for Windows raw-disk enumeration/open/write/elevation and native host integration.

Forbidden responsibilities: partition policy, artifact selection, hashing/signature policy, OrdaX layout rules, release semantics or separate user flow.

WSL is not used or required.
